<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.server.mapper.UserMapper">

    <resultMap id="UserMap" type="org.server.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="phone" property="phone"/>
        <result column="enabled" property="enabled"/>
        <result column="register_time" property="registerTime"/>
        <collection property="roles" ofType="org.server.entity.Role">
            <id column="rid" property="id"/>
            <result column="rname" property="name"/>
            <result column="remark" property="remark"/>
        </collection>
    </resultMap>

    <select id="getUserList" resultMap="UserMap">
        select * from view_user_role
        <if test="keyword != null">
            where username like concat('%',#{keyword},'%')
        </if>
    </select>

    <select id="getUserByUsername" resultType="org.server.entity.User">
        select * from tb_user where username=#{username}
    </select>

    <insert id="addUser" parameterType="org.server.entity.User">
        insert ignore into tb_user(username, password, phone)
        values (#{username}, #{password}, #{phone})
    </insert>

    <update id="updateUserEnabled">
        update tb_user set enabled=#{enabled} WHERE id=#{id}
    </update>

    <delete id="deleteUserById">
        delete from tb_user where id=#{id}
    </delete>

    <select id="getRolesByUid" resultType="org.server.entity.Role">
        select rid, rname, remark from view_user_role where id=#{uid}
    </select>

    <insert id="addRoleByUid">
        insert into tb_user_role values (#{uid}, #{rid})
    </insert>

    <delete id="deleteRoleByUid">
        delete from tb_user_role where uid=#{uid} and rid=#{rid}
    </delete>

    <delete id="deleteAllRoleByUid">
        delete from tb_user_role where uid=#{uid}
    </delete>
</mapper>
