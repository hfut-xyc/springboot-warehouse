server:
  port: 8081

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_warehouse?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    userName: root
    password: root

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.admin: DEBUG
